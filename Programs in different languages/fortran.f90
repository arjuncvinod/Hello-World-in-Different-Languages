program HelloWorld
  ! This is a simple Fortran program to print "Hello, World!"

  ! Declare character string for the message
  character(13) :: message

  ! Assign the message
  message = "Hello, World!"

  ! Print the message to the console
  write(*, *) message

  ! End the program
end program HelloWorld
